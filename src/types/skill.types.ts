export type SkillType =
  | "frontend"
  | "backend"
  | "soft"
  | "hard"
  | "fullstack"
  | "programmingl"
  | "database"
  | "tools";

export interface TypeConfig {
  title: string;
  class: string;
}
